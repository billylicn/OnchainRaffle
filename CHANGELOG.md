# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用语义化版本号。

## [未发布]

### 新增

- MIT 开源许可证、贡献指南、安全报告说明和 GitHub Issue/PR 模板。
- GitHub Actions 持续集成，以及可手动触发的 GitHub Pages 部署工作流。

### 变更

- 扩充 README 的项目边界、目录结构、部署、凭证兼容和开源协作说明。
- 完善忽略规则和环境变量示例，避免本地构建、测试报告、日志与 `.env` 文件进入仓库。

## [1.1.0] - 2026-09-04

### 新增

- 可编辑的编号、姓名双列表格，支持 Excel 整块粘贴、逐格编辑、键盘导航、增删行、清空和文件导入后继续修改。
- 奖项中文默认命名，新增奖项会自动选择下一个未使用的“一等奖”“二等奖”“三等奖”等名称。
- 目标区块链上时间及 Unix 时间戳展示，以及带时间戳核对的紧凑 `DrawProofV3`。
- 手动计算下一结果、自动运算、暂停、`2x` / `5x` 和直接开奖控制。
- 开奖计算页进入后保持未启动状态；首次点击“开始运算”才高亮哈希片段，并在短暂模糊运算动画后揭晓第一个结果。
- 始终展开的区块数据源选择，可在 PublicNode、LlamaRPC、1RPC 和自定义 Ethereum Mainnet RPC 之间选择。
- 开奖验证可信性说明，分步解释主网数据核对、名单摘要核对和算法重新计算。
- 未完成抽奖的新建确认，以及完成后保留配置模板、清除旧结果的新抽奖流程。

### 变更

- 名单编号现在必须从 `1` 连续递增；关闭表头识别与跳过，所有非空行都按参与者数据校验。
- 等待动画改为“锁定时当前区块高度 → 目标区块高度”的直观流程，只以 `等待确认 0/2`、`1/2`、`2/2` 展示后续确认。
- 算法说明明确使用 Ethereum Mainnet `chainId = 1`，并用面向普通参与者的文字和完整公式解释种子、拒绝采样与无放回抽取。
- 计算页不再自动开始开奖；默认每个奖项约 4 秒，展示速度不影响既定结果。
- 公证页顶部增加网络、目标区块高度、链上时间、参与总人数、奖项数量和中奖总名额摘要。
- 所有用户界面统一使用“区块高度”表述，并移除等待页的下次查询倒计时提示。
- 结果列表移除装饰性顺序号，只显示真实中奖编号与本机姓名。
- 所有新抽奖编号固定为 `1...N`；数字编号模式改为只填写参与总人数。
- V3 凭证使用参与总人数还原编号，不再保存完整编号数组和中奖数组；验证页重新计算并按奖项展示抽奖次序与中奖编号。
- 二维码统一为完整复算凭证，不再因参与人数增加而降级为摘要验证。

### 移除

- 移除旧版 `DrawProofV1`、`DrawProofV2` 和摘要凭证导入，仅支持最新 `DrawProofV3`；`CHAIN_DRAW_V1` 开奖算法与结果保持不变。

## [1.0.0] - 2026-09-04

### 新增

- 完整 JSON 公证凭证、压缩二维码与摘要二维码降级。
- 独立公证页，可核对 Ethereum Mainnet 区块哈希并重新执行抽奖算法。
- 中奖编号公开展示、姓名本机展示，以及源代码入口配置。
- README、端到端验收配置和完整响应式界面。

### 变更

- Excel 解析改为按需加载，降低首页初始脚本体积。
- 整体界面升级为浅色科幻审计风格，加入链上网格、ETH 轨道、哈希流和扫描光视觉。
- 重做等待与计算进度条，增加分段刻度、能量流动、区块脉冲和计算控制台动画。
- 优化桌面与手机端步骤条、二维码扫描和结果页布局，并增加移动端遮挡回归检查。

## [0.3.0] - 2026-09-04

### 新增

- Ethereum Mainnet RPC 校验、公共节点容错和自定义 RPC。
- 目标区块 `N + 2` 锁定、每 5 秒轮询与两个后继区块确认。
- `CHAIN_DRAW_V1` 拒绝采样、Partial Fisher-Yates 和计算动画。
- Seed 哈希链扩展：`Seedₙ₊₁ = SHA-256(Seedₙ)`。
- 锁定状态、本地恢复、取消重开和中奖结果页面。

## [0.2.0] - 2026-09-04

### 新增

- Excel 粘贴和文件导入、中文表头识别及逐行校验。
- 最多 200 人名单模式和最多 5000 人数字范围模式。
- 独立奖项名称、名额编辑与规范性检查。
- 简短算法说明和公平性边界提示。

## [0.1.0] - 2026-09-04

### 新增

- Vite、React、TypeScript 和 Hash 路由项目骨架。
- “链上开奖”响应式首页、专业浅色设计系统和区块主视觉。
- 静态部署基础配置与首个可构建版本。
