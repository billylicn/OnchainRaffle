# 安全说明

## 支持范围

安全修复优先面向当前 `main` 分支和最新发布版本。旧版公开凭证不保证被最新验证器继续接受；当前仅支持 `DrawProofV3`。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security > Report a vulnerability** 私密报告功能。若仓库尚未启用私密漏洞报告，请只创建一个不包含漏洞细节的 Issue，请求维护者提供私密联系方式。

报告中建议包含：

- 受影响版本或提交号
- 可复现步骤和最小测试数据
- 对开奖结果、凭证验证、隐私或 RPC 请求的影响
- 建议修复方式（如有）

不要在公开 Issue 中发布真实姓名、完整活动名单、私有 RPC 地址、访问令牌或尚未修复的利用细节。

## 项目安全边界

- 本项目是纯静态前端，不托管账户、数据库或服务端密钥。
- 浏览器会直接访问公开或用户配置的 Ethereum Mainnet RPC，因此前端无法隐藏 RPC 凭据。
- 姓名和本地会话保存在浏览器 `localStorage`，清理浏览器数据会删除这些信息。
- 区块哈希可复算机制不等同于链上事前承诺，具体边界见 README。
